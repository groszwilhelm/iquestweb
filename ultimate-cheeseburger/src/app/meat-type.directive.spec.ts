import { MeatTypeDirective } from './meat-type.directive';

describe('MeatTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new MeatTypeDirective();
    expect(directive).toBeTruthy();
  });
});
